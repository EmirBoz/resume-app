# =============================================================================
# CV-ANGULAR ENVIRONMENT CONFIGURATION
# =============================================================================
# ⚠️  CRITICAL: Change ALL default values for production!
# 🚫 NEVER commit .env files to Git!

# Server Configuration
PORT=4000
NODE_ENV=development

# =============================================================================
# DATABASE CONFIGURATION
# =============================================================================
# Development: Local MongoDB
MONGODB_URI=mongodb://localhost:27017/cv-app

# Production: MongoDB Atlas (REQUIRED for production)
# 🔴 IMPORTANT: 
# - Use M10+ cluster for production (not M0)
# - Configure specific IP whitelist (NOT 0.0.0.0/0)
# - Use strong password (32+ characters)
# MONGODB_URI=mongodb+srv://cv-production-user:YOUR_SECURE_PASSWORD@cv-app-production.xyz123.mongodb.net/cv-app?retryWrites=true&w=majority

# =============================================================================
# AUTHENTICATION & SECURITY
# =============================================================================
# JWT Secret - MUST be changed in production
# 🔴 Generate strong random string: openssl rand -base64 64
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
JWT_EXPIRES_IN=7d

# Admin Credentials - CHANGE THESE IN PRODUCTION!
# ⚠️ Default credentials are ONLY for development
ADMIN_USERNAME=admin
ADMIN_PASSWORD=admin123

# Production Example - Use strong credentials:
# ADMIN_USERNAME=admin_production_2024
# ADMIN_PASSWORD=SuperSecure$Admin9assw0rd2024!

# =============================================================================
# CORS CONFIGURATION
# =============================================================================
# Development origins
CORS_ORIGINS=http://localhost:4200,http://localhost:3000

# Production origins - Update with your domain:
# CORS_ORIGINS=https://yourdomain.com,https://www.yourdomain.com

# =============================================================================
# PRODUCTION EXAMPLE CONFIGURATION
# =============================================================================
# Copy and modify these values for production:
#
# NODE_ENV=production
# MONGODB_URI=mongodb+srv://cv-production-user:kJ8#mL9$nP2@wQ5&xR7^zA3*bC6!vE4(@cv-app-production.xyz123.mongodb.net/cv-app?retryWrites=true&w=majority
# JWT_SECRET=prod-jwt-secret-2024-very-long-random-string-with-special-chars
# ADMIN_USERNAME=admin_production_2024
# ADMIN_PASSWORD=SuperSecure$Admin9assw0rd2024!
# CORS_ORIGINS=https://cv-app-production.vercel.app,https://www.yourdomain.com
#
# 🔒 Security Checklist for Production:
# ✓ MongoDB Atlas M10+ cluster
# ✓ Specific IP whitelist (no 0.0.0.0/0)
# ✓ Strong database password (32+ chars)
# ✓ Unique JWT secret (64+ chars)
# ✓ Changed admin credentials
# ✓ HTTPS-only CORS origins
# ✓ Environment variables on platform